# Render Blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: comp4537-httponly-cookie
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
